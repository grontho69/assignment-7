1. JSX means JavaScript XML .its a syntex use in react.we can write html code in javascript by using JSX

2. difference between state and props---
  * state is a component data and we can change it 
  * props are a data that goes from parent to child and it cant be changed by the child


  3. useState is a react hook which we use to store data

  4.I can share state by keep the state in the parent and pass it to the child

  5.by using camlecase formate and function , react handels event